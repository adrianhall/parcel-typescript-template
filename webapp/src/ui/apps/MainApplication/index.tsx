import React from 'react';
import { VerticalLayout } from '~/lib/components'

const MainApplication: React.SFC<{}> = () => {
  return (
    <VerticalLayout>
      <h1>MainApplication</h1>
    </VerticalLayout>
  );
};

export default MainApplication;